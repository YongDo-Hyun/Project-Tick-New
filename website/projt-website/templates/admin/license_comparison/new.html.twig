{% extends 'base.html.twig' %}

{% block title %}New Comparison - Admin{% endblock %}

{% block body %}
    <h1>New Comparison Field</h1>

    <div class="card" style="max-width: 600px;">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <div class="admin-card-actions">
                <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
                <a href="{{ path('app_admin_license_comparison_index') }}" class="btn btn-secondary">Cancel</a>
            </div>
        {{ form_end(form) }}
    </div>
{% endblock %}
